tags:
  - alias
summary: Options
description: User alias info and suggestion. Used by the first extension screen when user opens the extension.
operationId: "options"
# TODO: Query param hostname, optional but recommended
responses:
  '200':
    description: Successful
    content: 
      application/json:
        schema:
          $ref: ../../../../components/schemas/alias/options/model.yml
      