tags:
  - account
summary: Cookie token
description: Get a one time use cookie to exchange it for a valid cookie in the web app
operationId: "cookieToken"
responses:
  '200':
    description: Successful
    content: 
      application/json:
        schema:
          $ref: ../../../components/schemas/user/cookie_token/model.yml
      
  '403':
    description: Forbidden
    content: 
      application/json:
        schema:
          $ref: ../../../components/schemas/error.yml
      
