type: object
properties:
  email:
    type: string
    format: email
    description: Email address used to register account
  code:
    type: string
    description: Activation code sent to email

required:
  - email
  - code
