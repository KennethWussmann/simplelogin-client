tags:
  - custom-domain
summary: Get deleted aliases
description: Get all deleted aliases for a custom domain (trash).
operationId: {{ opId() }}
parameters:
  - $ref: "@parameters/pathCustomDomainId.yml"
responses:
  "200":
    description: Successful
    content: {{ mixin("@mixins/request/json.yml", "@schemas/custom_domain/{custom_domain_id}/trash/model.yml") }}
  "404":
    description: Not found
    content: {{ mixin("@mixins/request/json.yml", "@schemas/error.yml") }}
